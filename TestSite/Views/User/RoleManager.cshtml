@using TestSite.Models
@{
    ViewBag.Title = "RoleManager";
    User actualluser = new User();
    if ((User) Session["User"] != null)
    {
        actualluser = (User) Session["User"];
    }
}


<br /><br /><br />
        @using(Html.BeginForm("DeleteUser", "User"))
    {
        <form class="form-horizontal">
            <fieldset>
                <legend>Nutzer löschen</legend>
                <div class="form-group">
                    <label for="inputEmail" class="col-lg-2 control-label">Email</label>
                    <div class="col-lg-10">
                        <input type="text" class="form-control" id="inputEmail" name="Mail" placeholder="Email">
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-10 col-lg-offset-2">
                        <button type="reset" class="btn btn-default">Abbrechen</button>
                        <button type="submit" class="btn btn-primary">Löschen</button>
                    </div>
                </div>
            </fieldset>
        </form>
    }


   
